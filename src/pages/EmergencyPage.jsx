import React from 'react';

const EmergencyPage = () => {
    return (
        <div className="glass-panel" style={{ padding: '2rem', height: '100%' }}>
            <h2>🚨 Nominee Emergency Panel</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Critical information and access protocols for your nominees.</p>
        </div>
    );
};

export default EmergencyPage;
