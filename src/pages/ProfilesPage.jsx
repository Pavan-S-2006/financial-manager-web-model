import React from 'react';

const ProfilesPage = () => {
    return (
        <div className="glass-panel" style={{ padding: '2rem', height: '100%' }}>
            <h2>👨‍👩‍👧 Profiles & Managerial Controls</h2>
            <p style={{ color: 'var(--text-secondary)' }}>Manage family access, roles, and privacy settings.</p>
        </div>
    );
};

export default ProfilesPage;
